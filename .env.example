# ResumRank AI — Environment Variables
# No API key needed. Copy this to .env and set SECRET_KEY.

# App Environment
APP_ENV=production

# Flask secret key (required in production)
SECRET_KEY=your-random-secret-key-here

# Server port
PORT=5000
# No GEMINI_API_KEY needed. App uses local spaCy NLP.
